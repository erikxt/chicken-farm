type Subject {
  id: ID!
  subjectName: String
  subjectAnswer: String
  difficulty: Int
  assembleIds: [String]
  labelNames: [String]
  primaryCategoryId: Int
}

type Query {
    subject(id: ID!): Subject
}

schema {
  query: Query
}